import 'package:flutter/material.dart';

class AppColors {
  // Paleta de bem-estar - tons amarelados e calorosos
  static const primary = Color(
    0xFFFFB300,
  ); // Amarelo dourado principal - energia e positividade
  static const primaryLight = Color(0xFFFFD54F); // Amarelo claro
  static const primaryDark = Color(0xFFF57F17); // Amarelo escuro/laranja

  // Cores secundárias de bem-estar amareladas
  static const serenity = Color(0xFFFFE082); // Amarelo suave - tranquilidade
  static const balance = Color(0xFFFFCC02); // Amarelo vibrante - equilíbrio
  static const energy = Color(0xFFFF8F00); // Laranja quente - vitalidade
  static const peace = Color(0xFFFFF8E1); // Amarelo muito claro - paz
  static const vitality = Color(0xFFFFD600); // Amarelo puro - vitalidade
  static const mindfulness = Color(0xFFFFAB00); // Âmbar - consciência

  // Cores complementares (para contraste)
  static const complement = Color(
    0xFF6A4C93,
  ); // Roxo suave - complemento do amarelo
  static const complementLight = Color(0xFF9C89B3); // Roxo claro
  static const accent = Color(0xFF2196F3); // Azul para acentos importantes

  // Cores neutras com base amarelada
  static const background = Color(0xFFFFFDF7); // Fundo amarelo muito claro
  static const surface = Color(0xFFFFFFFF); // Branco puro
  static const surfaceLight = Color(0xFFFFF9E6); // Amarelo quase imperceptível
  static const surfaceWarm = Color(0xFFFFFBF0); // Bege amarelado muito claro

  // Estados com harmonia amarela
  static const success = Color(0xFF8BC34A); // Verde lima (compatível)
  static const warning = Color(0xFFFF9800); // Laranja (família do amarelo)
  static const error = Color(0xFFE57373); // Vermelho suave
  static const info = Color(0xFF42A5F5); // Azul suave

  // Texto com melhor contraste
  static const textPrimary = Color(0xFF2D2D2D); // Cinza escuro quente
  static const textSecondary = Color(0xFF5D4E37); // Marrom suave
  static const textLight = Color(0xFF8D6E63); // Marrom claro
  static const textOnPrimary = Color(
    0xFF1A1A1A,
  ); // Texto escuro para fundos amarelos
}
