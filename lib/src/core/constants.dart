import 'package:flutter/material.dart';

class AppColors {
  // Paleta moderna de bem-estar - tons de verde-azulado (teal) e complementares
  static const primary = Color(
    0xFF26A69A,
  ); // Teal principal - calmo e equilibrado
  static const primaryLight = Color(0xFF4DB6AC); // Teal claro
  static const primaryDark = Color(0xFF00695C); // Teal escuro

  // Cores secundárias de bem-estar
  static const serenity = Color(0xFF80CBC4); // Verde-água suave - tranquilidade
  static const balance = Color(0xFF4DD0E1); // Cyan claro - equilíbrio
  static const energy = Color(0xFF66BB6A); // Verde natural - vitalidade
  static const peace = Color(0xFFE0F2F1); // Verde muito claro - paz
  static const vitality = Color(0xFF42A5F5); // Azul vibrante - clareza mental
  static const mindfulness = Color(0xFF7986CB); // Azul suave - consciência

  // Cores complementares para acentos
  static const complement = Color(0xFFFF7043); // Coral suave - calor humano
  static const complementLight = Color(0xFFFFAB91); // Coral claro
  static const accent = Color(0xFF5C6BC0); // Índigo para destaques importantes

  // Cores neutras modernas
  static const background = Color(0xFFFAFAFA); // Cinza muito claro
  static const surface = Color(0xFFFFFFFF); // Branco puro
  static const surfaceLight = Color(0xFFF1F8E9); // Verde muito claro
  static const surfaceWarm = Color(0xFFF3E5F5); // Lavanda muito sutil

  // Estados com harmonia natural
  static const success = Color(0xFF66BB6A); // Verde natural
  static const warning = Color(0xFFFFB74D); // Laranja suave
  static const error = Color(0xFFEF5350); // Vermelho suave
  static const info = Color(0xFF42A5F5); // Azul informativo

  // Texto com contraste otimizado
  static const textPrimary = Color(0xFF212121); // Cinza muito escuro
  static const textSecondary = Color(0xFF616161); // Cinza médio
  static const textLight = Color(0xFF9E9E9E); // Cinza claro
  static const textOnPrimary = Color(
    0xFFFFFFFF,
  ); // Texto branco para fundos coloridos
}
