import 'package:flutter/material.dart';

class AppColors {
  // Paleta de bem-estar - tons serenos e relaxantes
  static const primary = Color(
    0xFF4CAF50,
  ); // Verde principal - crescimento e harmonia
  static const primaryLight = Color(0xFF81C784); // Verde claro
  static const primaryDark = Color(0xFF388E3C); // Verde escuro

  // Cores secundárias de bem-estar
  static const serenity = Color(0xFF64B5F6); // Azul sereno - calma
  static const balance = Color(0xFF9C27B0); // Roxo equilíbrio - espiritualidade
  static const energy = Color(0xFFFF9800); // Laranja energia - vitalidade
  static const peace = Color(0xFF607D8B); // Cinza azulado - paz
  static const vitality = Color(0xFF8BC34A); // Verde lima - vitalidade
  static const mindfulness = Color(0xFF795548); // Marrom - grounding

  // Cores neutras
  static const background = Color(0xFFF8FDF8); // Fundo verde muito claro
  static const surface = Color(0xFFFFFFFF); // Branco puro
  static const surfaceLight = Color(0xFFF5F9F5); // Verde muito claro

  // Estados
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFFC107);
  static const error = Color(0xFFF44336);
  static const info = Color(0xFF2196F3);

  // Texto
  static const textPrimary = Color(0xFF2E2E2E);
  static const textSecondary = Color(0xFF666666);
  static const textLight = Color(0xFF999999);
}
